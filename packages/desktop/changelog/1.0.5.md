# 1.0.5 — 2026-09-03

### Fixed

- Images dragged in from a browser: the request now asks the way that browser would, so
  boards that only serve images to the page they belong to no longer refuse it.
- A drag that fails says what went wrong — unreachable host, closed connection, bad
  certificate — instead of one flat "Could not reach that link".
