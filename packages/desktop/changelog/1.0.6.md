# 1.0.6 — 2026-09-03

### Added

- Drag queue rows by the handle to reorder them. Posts are created top to bottom.
- Closing with images still in the queue asks first. An empty queue closes as before.

### Fixed

- Images dragged from a browser now resolve over DNS-over-HTTPS, like the browser does —
  a site your ISP's DNS won't look up no longer fails here.
