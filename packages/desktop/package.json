{
  "name": "desktop",
  "version": "1.0.2",
  "private": true,
  "description": "Pubooru desktop uploader — the upload page, run locally so image compression uses a real CPU",
  "main": "./out/main/index.js",
  "author": "Pubooru",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "typecheck": "tsc --noEmit",
    "clean": "node -e \"for (const dir of ['dist', 'out']) require('node:fs').rmSync(dir, { recursive: true, force: true })\"",
    "package": "npm run clean && electron-vite build && electron-builder --win --config electron-builder.yml"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4",
    "sharp": "^0.35.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^5",
    "electron": "44.0.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vite": "^7"
  }
}
